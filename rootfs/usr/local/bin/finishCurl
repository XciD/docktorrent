#!/bin/bash

isFolder="$1"
label="$2"
shift 2

seedboxContainerId=CONTAINERID
host=HOST

curl -v --get --data-urlencode "isFolder=$isFolder" --data-urlencode "label=$label" --data-urlencode "seedboxContainerId=$seedboxContainerId" --data-urlencode "fileName=$*"  $host/api/nas/download